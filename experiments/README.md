# Experiments

Small experiments comparing kernel implementations. Begin with split versus
interleaved complex storage for copy, elementwise add, and elementwise multiply.

Keep each experiment's configuration and invocation alongside its code. Write
generated measurements to `results/` and raw profiler artifacts to `profiles/`
at the repository root.
