# Kernels

Implementations of complex-valued operations. Initial operations:

- Copy: `out = x`.
- Elementwise add: `out = x + y`.
- Elementwise multiply: `out = x * y` (complex multiplication).

Compare interleaved real/imaginary storage with separate contiguous real and
imaginary tensors. Add operation and backend directories when implementations
are introduced.
